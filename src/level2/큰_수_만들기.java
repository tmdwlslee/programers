package level2;

public class 큰_수_만들기 { // 시간 안에 못품

    public String solution(String number, int k) {
        StringBuffer answer = new StringBuffer(number);

        int index = 1;
        while (k > 0){
            if(index > 1){

            }
        }

        return String.valueOf(0);
    }
}
